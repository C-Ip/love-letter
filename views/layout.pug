doctype html
html
  head
    title= title
    link(href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.7/cerulean/bootstrap.min.css", rel="stylesheet", integrity="sha384-zF4BRsG/fLiTGfR9QL82DrilZxrwgY/+du4p/c7J72zZj+FLYq4zY00RylP9ZjiT", crossorigin="anonymous")
    link(rel='stylesheet', href='/stylesheets/style.css')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js')
    script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js')
    script(src='/socket-io/socket.io.js')
    script.
      $(document).ready(function(){
        $('[data-toggle="popover"]').popover();
      });

  body
    nav.navbar.navbar-inverse.navbar-fixed-top
     .container
       .navbar-header
         a.navbar-brand(href='/') Home
         a#rules.navbar-brand.dropdown-toggle(data-toggle='dropdown') How To Play
         ul#menu.dropdown-menu(role='menu', aria-labelledby='dropdownMenu1')
           p 1. Each player is dealt one card at the beginning of the game.
           p 2. Each player draws a card at the beginning of their turn.
           p 3. You will then choose one card to discard from your hand.
           p 4. Perform the action on the discarded card.
           p 5. That is the end of a player's turn.
           p#special *Win condition: If only one player is still in the round, they win. Otherwise all players reveal their hands. The player with the highest ranked card wins the round.
       a.navbar-brand.pull-right(href='/registration' data-toggle='modal' role='button',data-target='#modalRegister') Register
       a.navbar-brand.pull-right(href='#' data-toggle='modal' role='button' data-target='#modalLogin') Login

div(id='modalRegister',class='modal fade',tabindex ='-1',role ='dialog')
    div(class='modal-content')
      form(method='POST' action='/registration')
      div(class='modal-header')
        button(type='button',class='close',data-dismiss='modal') close
        h4(class='modal-titles') Register
      div(class='modal-body')
        div.form-group
        label(for='username') Username
        input.form-control(type='text' id='usernameReg' placeholder='enter username')
        div.form-group
        label(for='password') Password
        input.form-control(type='password' id ='passwordReg' placeholder='enter password')
        div.form-group
        label(for='confirm') Confirm Password
        input.form-control(type='password' id='confirmPassword' placeholder='confirm password')
      div(class='modal-footer')
        button.btn.btn-primary(id='register',type='submit') Register


div(id = 'modalLogin',class='modal fade',tabindex = '-1',role='dialog')
    div(class='modal-content')
      div(class='modal-header')
        button(type='button',class= 'close',data-dismiss = 'modal') close
        h4(class='modal-titles') Login
      div(class='modal-body')
        form(method='POST' action = '/login')
        div.form-group
        label(for='username') Username
        input#username_login(type='text',placeholder='enter username')
        div.form-group
        label(for='password') Password
        input#password_login(type='password',placeholder='enter password')
      div(class='modal-footer')
        button.btn.btn-primary(id ='login',type='submit') Login
block content
